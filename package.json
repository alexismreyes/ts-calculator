{
  "name": "calculator-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp index.html dist/index.html && cp style.css dist/style.css && cp index.js dist/index.js",
    "watch": "tsc --watch",
    "dev": "live-server --entry-file=dist/index.html --mount=/:./dist",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
